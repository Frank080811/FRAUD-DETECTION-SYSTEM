FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV PYTHONPATH=/app

CMD ["bash", "-c", "python workers/alert_worker.py 2>&1 | tee /tmp/log.txt; echo '❌ WORKER CRASHED'; sleep 1000"]
